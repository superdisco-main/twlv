---
description: When the user commands to "execute the solution", it configures the environment based on the rules and then runs the solution
globs: 
alwaysApply: false
---
# Environment Setup and Solution Execution Rules

## Required Environment Variables

Before running the solution, ensure that a `.env` file exists at the root directory with the following variables:

```
LANGSMITH_API_KEY=
OPENAI_API_KEY=
LLM_PROVIDER=
TWELVE_LABS_API_KEY=
HOST_PUBLIC_DIR=
```

## Running the Solution

To execute the solution:

1. Activate the virtual environment and Run the application:
   ```
   . venv/bin/activate
   python -m jockey terminal
   ``` 